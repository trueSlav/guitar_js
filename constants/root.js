// Выборка DOM-узлов, в которые будем рендерить.
const ROOT_PRODUCTS = document.querySelector('#products');
const ROOT_HEADER = document.querySelector('#header');
const ROOT_SHOPPING = document.querySelector('#shopping');